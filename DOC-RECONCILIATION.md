# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats
the code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. No code was changed and no builds or tests were run. Its purpose is to provide an
auditable record of what was checked, what was wrong, and what was fixed for any reviewer scanning
this branch.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `backend/main.py:372–382` mounts routers for auth, health, settings, ingest, incidents,
clusters, reports, and webhooks. `src/App.tsx:24–47` lazily loads Dashboard, Incidents, Clusters,
Reports, and Settings pages — all of which exist as `.tsx` files. Services for Jira, Slack,
Statuspage, Zendesk, Ollama clustering, embedding, DOCX generation, and metrics are all present under
`backend/services/`. The README intro accurately describes a local-first desktop app that ingests
from the four sources, clusters with Ollama, and generates DOCX reports.

### 2. Current state

**Status:** `consistent`
**Evidence:** Fourteen backend test files exist (test_phase0–6, test_contract_api, test_e2e_phase4,
test_integration, test_metrics_endpoint, test_statuspage, test_zendesk, test_helpers). Frontend has
`tests/stronghold.test.ts` and `vitest` configured in `package.json`. Recent git log shows active
feature merges (`feat(zendesk)`, `feat(statuspage)`, security fixes, CI updates) through the
reconciliation HEAD. No stubs or TODO placeholders were found in the core service layer. The README
does not make forward-looking promises that code contradicts.

### 3. Stack

**Status:** `drifted` (one item)
**Evidence:** `backend/pyproject.toml:6` declares `requires-python = ">=3.12"`.
README.md stated `Python 3.11+` under Prerequisites.

**Fix applied — `README.md`:**

> Before: `Python 3.11+ (for the FastAPI backend sidecar)`
> After: `Python 3.12+ (for the FastAPI backend sidecar)`

All other stack entries (Tauri 2 + Rust, React + TypeScript + Vite, Python + FastAPI, Ollama,
SQLite, Recharts, TanStack Query, Tauri Stronghold) are confirmed by `src-tauri/Cargo.toml`,
`package.json`, `backend/pyproject.toml`, and source imports.

### 4. How to run

**Status:** `drifted` (two items)

**Item A — backend install command:**
`backend/requirements.txt` does not exist (verified via Glob search). The project uses
`backend/pyproject.toml` as its Python package manifest. README.md's Installation section said
`pip install -r backend/requirements.txt`, which would fail. The correct command (per
`QUICKSTART.md:9–10` and `backend/README.md:18–19`) is:

```
cd backend && python3 -m venv .venv && .venv/bin/pip install -e ".[dev]" && cd ..
```

**Fix applied — `README.md` Installation block:**

> Before: `pip install -r backend/requirements.txt`
> After: `cd backend && python3 -m venv .venv && .venv/bin/pip install -e ".[dev]" && cd ..`

**Item B — dev launch command:**
`package.json:7` defines `"dev": "vite"` — plain Vite only, no Tauri shell, no Python sidecar.
README.md's Run section said `pnpm dev`, which starts only the frontend server. The full desktop
app requires the Python sidecar and the Tauri shell; `QUICKSTART.md:45–52` and
`backend/README.md:34–36` both cite `bash scripts/dev.sh` as the repo-preferred full-launch path.
`src-tauri/tauri.conf.json:6` confirms the bundled sidecar binary is `binaries/incident-workbench-api`.

**Fix applied — `README.md` Run section:**

> Before: ` ```bash\npnpm dev\n``` `
> After: ` ```bash\nbash scripts/dev.sh\n``` ` with a one-line note explaining `pnpm dev` covers frontend only.

### 5. Known risks

**Status:** `consistent`
**Evidence:** The README does not have a standalone risks section; its prose and the `AGENTS.md`
portfolio-context block describe the local-first, Stronghold-backed credential posture. No
contradictions found between the stated posture and code: `src/utils/stronghold.ts` implements vault
unlock/reset via Tauri Stronghold, and `backend/security/` contains auth, CSRF, RBAC, and
idempotency modules.

### 6. Next move

**Status:** `unverifiable`
**Evidence:** No ROADMAP or STATUS file exists. The README has no roadmap section. The
`AGENTS.md` portfolio-context block mentions "Resolve the existing PR-template and lockfile drift
separately" — that item appears stale (git status at reconciliation HEAD is clean). The claim is
not verifiable from code alone; no edit was made.

---

## Contradictions for Manual Review

**`AGENTS.md` — portfolio-context block (not edited):**

- `AGENTS.md:56`: "Current uncommitted files are unrelated PR-template and lockfile work" — git
  status at HEAD `5da0d4d` is clean; this sentence is stale. A human should update or remove it
  from the portfolio-context block.

**`docs/LOCAL-CHECKOUT-DISPOSITION.md` — source description (edited in this pass):**

- Line 107 previously said "from Jira or Slack" omitting Statuspage and Zendesk, which are both
  fully implemented (see `backend/services/statuspage_client.py`,
  `backend/services/zendesk_client.py`, `backend/routers/ingest.py`,
  `feat(statuspage)` and `feat(zendesk)` commits). Fixed inline.

---

## Footer

**Reconciled against HEAD:** `5da0d4dd6216d22372e8ffc23dcf4208b0269e1e`
**Branch:** `codex/docs/truth-up-2026-06-02`
**Run date/time:** `2026-06-02 19:33:24 PDT`
